## Cena principal — entry point do jogo.
## Carrega a primeira cena e gerencia a UI persistente.
extends Node

@onready var dialog_box = $DialogBox
@onready var pause_menu = $PauseMenu


func _ready() -> void:
	pass
